---
phase: 18-critical-bugs
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/lib/session/__tests__/tiptap-render.test.ts
autonomous: true
requirements:
  - BUG-01

must_haves:
  truths:
    - "A failing test exists for contentToHtml() before implementation begins"
    - "Test covers Tiptap JSON object input, HTML string passthrough, and null/undefined input"
  artifacts:
    - path: "src/lib/session/__tests__/tiptap-render.test.ts"
      provides: "Failing unit tests for contentToHtml helper"
      exports: []
  key_links:
    - from: "src/lib/session/__tests__/tiptap-render.test.ts"
      to: "src/lib/session/tiptap-render.ts"
      via: "import { contentToHtml } from '../tiptap-render'"
      pattern: "contentToHtml"
---

<objective>
Create failing unit tests for the `contentToHtml()` helper that will be implemented in Plan 02.

Purpose: Nyquist compliance — BUG-01 requires a new helper function. Tests must be written (RED) before the implementation (GREEN) so that the executor in Plan 02 has a clear behavioral contract to satisfy.

Output: `src/lib/session/__tests__/tiptap-render.test.ts` — a failing test file that imports from `src/lib/session/tiptap-render.ts` (which does not yet exist).
</objective>

<execution_context>
@/home/dc/.claude/get-shit-done/workflows/execute-plan.md
@/home/dc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/session/recap-screen.tsx
</context>

<interfaces>
<!-- Pattern from existing test files in this project -->
<!-- src/lib/templates/__tests__/export-schema.test.ts format: -->
```typescript
import { describe, it, expect } from 'vitest';
import { functionUnderTest } from '../module-under-test';

describe('functionUnderTest', () => {
  it('description of behavior', () => {
    expect(functionUnderTest(input)).toBe(expectedOutput);
  });
});
```

<!-- Tiptap JSON document structure (what the DB may contain): -->
```typescript
// A minimal Tiptap JSON document with a paragraph
const tiptapJsonDoc = {
  type: "doc",
  content: [
    {
      type: "paragraph",
      content: [{ type: "text", text: "Hello world" }]
    }
  ]
};
```

<!-- The module to be created in Plan 02: -->
// src/lib/session/tiptap-render.ts
// export function contentToHtml(content: unknown): string
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1: Write failing tests for contentToHtml</name>
  <files>src/lib/session/__tests__/tiptap-render.test.ts</files>
  <behavior>
    - Test 1: string input — `contentToHtml("&lt;p&gt;Hello&lt;/p&gt;")` returns `"&lt;p&gt;Hello&lt;/p&gt;"` unchanged (HTML string passthrough)
    - Test 2: Tiptap JSON object input — `contentToHtml({ type: "doc", content: [{ type: "paragraph", content: [{ type: "text", text: "Hello" }] }] })` returns a string containing `"Hello"` and wrapping HTML (not `"[object Object]"`)
    - Test 3: null input — `contentToHtml(null)` returns `""`
    - Test 4: undefined input — `contentToHtml(undefined)` returns `""`
    - Test 5: malformed object (not a valid Tiptap doc) — `contentToHtml({ broken: true })` returns `""` without throwing
  </behavior>
  <action>
    Create `src/lib/session/__tests__/tiptap-render.test.ts` with all five tests above. Import `contentToHtml` from `../tiptap-render` (which does not exist yet — tests MUST fail at this point).

    Use `import { describe, it, expect } from 'vitest'` — consistent with all other test files in the project.

    For Test 2, assert the result does NOT equal `"[object Object]"` and DOES contain the text "Hello". Do not assert exact HTML — `generateHTML` output format may vary by extension config.

    After writing the file, run `bun run test src/lib/session/__tests__/tiptap-render.test.ts` and confirm it FAILS (import error or module not found). This RED state is the goal of this plan.
  </action>
  <verify>
    <automated>bun run test src/lib/session/__tests__/tiptap-render.test.ts 2>&1 | grep -E "FAIL|Cannot find|failed" | head -5</automated>
  </verify>
  <done>Test file exists, imports from `../tiptap-render`, and fails because that module does not yet exist. All 5 test cases are present with correct behavioral assertions.</done>
</task>

</tasks>

<verification>
- `src/lib/session/__tests__/tiptap-render.test.ts` exists
- Running `bun run test src/lib/session/__tests__/tiptap-render.test.ts` exits with error (module not found / import failure)
- All 5 test cases present covering: HTML passthrough, Tiptap JSON object, null, undefined, malformed object
</verification>

<success_criteria>
Failing test file in place. Plan 02 can now execute the GREEN phase by creating the implementation that makes these tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/18-critical-bugs/18-01-SUMMARY.md`
</output>
