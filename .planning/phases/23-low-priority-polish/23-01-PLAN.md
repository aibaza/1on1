---
phase: 23-low-priority-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/en/auth.json
  - messages/ro/auth.json
  - messages/en/history.json
  - messages/ro/history.json
  - src/app/(dashboard)/settings/audit-log/audit-log-client.tsx
  - src/app/(auth)/forgot-password/page.tsx
  - src/components/people/people-table-columns.tsx
  - src/components/action-items/action-items-page.tsx
autonomous: true
requirements: [POL-01, POL-02, POL-03, POL-04, POL-05, POL-08, POL-09]

must_haves:
  truths:
    - "Registration company name field shows 'Acme Corp' placeholder in English"
    - "Audit log shows 'AI Pipeline Completed' not 'Ai Pipeline Completed'"
    - "Forgot-password page card is vertically centered"
    - "People list shows no badge for active users, only badges for pending/deactivated"
    - "Action items COMPLETED divider text is 12px (text-xs) not 10px"
    - "History search placeholder shows 'Search...' without truncation"
  artifacts:
    - path: "messages/en/auth.json"
      provides: "Updated company placeholder"
      contains: "Acme Corp"
    - path: "messages/en/history.json"
      provides: "Shortened search placeholder"
      contains: "Search..."
    - path: "src/app/(dashboard)/settings/audit-log/audit-log-client.tsx"
      provides: "Acronym-aware title case formatter"
      contains: "ACRONYMS"
    - path: "src/components/people/people-table-columns.tsx"
      provides: "Conditional badge rendering"
      contains: "return null"
    - path: "src/components/action-items/action-items-page.tsx"
      provides: "Updated divider font size"
      contains: "text-xs"
  key_links: []
---

<objective>
Apply 6 independent copy and styling fixes: registration placeholder, audit log acronym casing, forgot-password centering verification, people list active badge hiding, action items divider font size, and history search placeholder.

Purpose: Professional polish across registration, settings, people, action items, and history pages.
Output: Updated translation files and component tweaks.
</objective>

<execution_context>
@/home/dc/.claude/get-shit-done/workflows/execute-plan.md
@/home/dc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-low-priority-polish/23-CONTEXT.md
@.planning/phases/23-low-priority-polish/23-RESEARCH.md
@.planning/phases/23-low-priority-polish/23-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix i18n placeholders and audit log acronym casing</name>
  <files>messages/en/auth.json, messages/ro/auth.json, messages/en/history.json, messages/ro/history.json, src/app/(dashboard)/settings/audit-log/audit-log-client.tsx</files>
  <read_first>
    - messages/en/auth.json (current companyPlaceholder value at line 28)
    - messages/ro/auth.json (current companyPlaceholder value at line 28)
    - messages/en/history.json (current searchPlaceholder value at line 5)
    - messages/ro/history.json (current searchPlaceholder value at line 5)
    - src/app/(dashboard)/settings/audit-log/audit-log-client.tsx (formatActionLabel function around line 71-76)
  </read_first>
  <action>
**POL-01 — Registration placeholder:**
In `messages/en/auth.json` line 28, change `"companyPlaceholder": "Acme Inc"` to `"companyPlaceholder": "Acme Corp"`.
In `messages/ro/auth.json` line 28, verify value is already `"Acme SRL"` — no change needed.

**POL-08 — History search placeholder:**
In `messages/en/history.json` line 5, change `"searchPlaceholder": "Search notes, answers, talking points..."` to `"searchPlaceholder": "Search..."`.
In `messages/ro/history.json` line 5, change `"searchPlaceholder": "Caută note, răspunsuri, puncte de discuție..."` to `"searchPlaceholder": "Cauta..."`.

**POL-02 — Audit log acronym casing:**
In `audit-log-client.tsx`, find the `formatActionLabel` function (around line 71-76). The current fallback does:
```typescript
action.split("_").map((w) => w.charAt(0).toUpperCase() + w.slice(1)).join(" ")
```
Add an acronym set BEFORE the function and update the map callback:
```typescript
const ACRONYMS: ReadonlySet<string> = new Set(["ai", "rls", "api"]);
```
Then in the `.map()` callback, replace the title-case logic with:
```typescript
.map((w) => ACRONYMS.has(w) ? w.toUpperCase() : w.charAt(0).toUpperCase() + w.slice(1))
```
This ensures "ai_pipeline_completed" renders as "AI Pipeline Completed" instead of "Ai Pipeline Completed".
  </action>
  <verify>
    <automated>cd /home/dc/work/1on1 && grep -q '"Acme Corp"' messages/en/auth.json && grep -q '"Search\.\.\."' messages/en/history.json && grep -q '"Cauta\.\.\."' messages/ro/history.json && grep -q 'ACRONYMS' src/app/\(dashboard\)/settings/audit-log/audit-log-client.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - messages/en/auth.json contains `"companyPlaceholder": "Acme Corp"`
    - messages/ro/auth.json contains `"companyPlaceholder": "Acme SRL"` (unchanged)
    - messages/en/history.json contains `"searchPlaceholder": "Search..."`
    - messages/ro/history.json contains `"searchPlaceholder": "Cauta..."`
    - audit-log-client.tsx contains `const ACRONYMS` with "ai", "rls", "api"
    - audit-log-client.tsx contains `ACRONYMS.has(w) ? w.toUpperCase()`
  </acceptance_criteria>
  <done>EN placeholder is "Acme Corp", history search is "Search..."/"Cauta...", and audit log acronyms render in all-caps.</done>
</task>

<task type="auto">
  <name>Task 2: Fix forgot-password centering, active badge, and action items divider</name>
  <files>src/app/(auth)/forgot-password/page.tsx, src/app/(auth)/layout.tsx, src/components/people/people-table-columns.tsx, src/components/action-items/action-items-page.tsx</files>
  <read_first>
    - src/app/(auth)/layout.tsx (verify centering classes applied to all auth children)
    - src/app/(auth)/forgot-password/page.tsx (check if already centered via layout)
    - src/app/(auth)/login/page.tsx (reference: what centering looks like on login)
    - src/components/people/people-table-columns.tsx (status cell around line 211-218)
    - src/components/action-items/action-items-page.tsx (text-[10px] at lines ~361 and ~460)
  </read_first>
  <action>
**POL-03 — Forgot-password centering:**
The shared `(auth)/layout.tsx` applies `flex min-h-screen items-center justify-center` to all auth children. Read the forgot-password page and the login page. If both render a bare `<Card>` inside the layout with no extra wrapper overriding centering, this is already working — no code change needed, just verify. If the forgot-password page has a different wrapper (e.g., missing `w-full max-w-md` or extra container div), replicate the login page's pattern exactly.

**POL-04 — People list active badge:**
In `people-table-columns.tsx`, find the status cell definition (around line 211-218). Currently it renders a Badge for every status. Change the cell function to return null when status is "active":
```typescript
cell: ({ row }) => {
  const status = row.original.status;
  if (status === "active") return null;
  return (
    <Badge variant="outline" className={getStatusColor(status)}>
      {t(`table.${status}`)}
    </Badge>
  );
},
```
This hides the badge for active users entirely. Only pending/deactivated users show a badge.

**POL-05 — Action items COMPLETED divider:**
In `action-items-page.tsx`, find BOTH occurrences of `text-[10px]` (around lines 361 and 460 — one for "My Items" completed section, one for "My Reports" completed section). Change each from `text-[10px]` to `text-xs` (which is 12px in Tailwind, per UI-SPEC decision to use scale token instead of raw pixel). The UI-SPEC specifies `text-xs` (12px) with `font-semibold`, `uppercase`, `tracking-wider`, and `text-muted-foreground/60`.
  </action>
  <verify>
    <automated>cd /home/dc/work/1on1 && grep -c 'text-\[10px\]' src/components/action-items/action-items-page.tsx | grep -q '^0$' && grep -c 'text-xs' src/components/action-items/action-items-page.tsx | grep -qv '^0$' && grep -q 'return null' src/components/people/people-table-columns.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - action-items-page.tsx contains zero occurrences of `text-[10px]`
    - action-items-page.tsx contains `text-xs` in the COMPLETED divider sections
    - people-table-columns.tsx status cell contains `if (status === "active") return null` or equivalent early return
    - people-table-columns.tsx still renders Badge for non-active statuses
    - forgot-password page is centered (either already via layout or fixed to match login)
  </acceptance_criteria>
  <done>Action items divider uses text-xs, active users show no badge, forgot-password is vertically centered.</done>
</task>

</tasks>

<verification>
- `bun run typecheck` passes
- `bun run lint` passes
- `bun run build` succeeds
</verification>

<success_criteria>
- Registration placeholder shows "Acme Corp" (EN)
- Audit log acronyms render in all-caps (AI, RLS, API)
- Forgot-password page is vertically centered
- Active users show no status badge in people list
- COMPLETED divider uses text-xs (12px) at both locations
- History search placeholder is "Search..." / "Cauta..."
</success_criteria>

<output>
After completion, create `.planning/phases/23-low-priority-polish/23-01-SUMMARY.md`
</output>
