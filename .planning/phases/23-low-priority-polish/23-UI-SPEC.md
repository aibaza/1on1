---
phase: 23
slug: low-priority-polish
status: draft
shadcn_initialized: true
preset: new-york (neutral base)
created: 2026-03-20
---

# Phase 23 — UI Design Contract

> Visual and interaction contract for 8 independent polish fixes. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (new-york style) |
| Preset | neutral base color, CSS variables, Tailwind CSS 4 |
| Component library | Radix UI (via shadcn) |
| Icon library | Lucide |
| Font | Geist Sans (--font-geist-sans), Geist Mono (--font-geist-mono) |

No new components or shadcn additions required. All changes modify existing components and translation files.

---

## Spacing Scale

Declared values (inherited from existing design system, multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing |
| md | 16px | Default element spacing |
| lg | 24px | Section padding |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions: none — Phase 23 makes no spacing changes.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 (regular) | 1.5 |
| Label | 12px | 600 (semibold) | 1.4 |
| Heading | 20px | 600 (semibold) | 1.2 |
| Display | 28px | 600 (semibold) | 1.2 |

Two weights only: 400 (regular) for body text, 600 (semibold) for labels, headings, and display.

### Phase-Specific Typography Decisions

| Item | Element | Spec |
|------|---------|------|
| POL-05 | COMPLETED section divider text | `text-xs` (12px), font-semibold (600), uppercase, tracking-wider, color muted-foreground/60 |

This is the only typography change in the phase. The divider moves from `text-[10px]` to `text-xs` (12px). Using the scale token `text-xs` instead of a raw pixel value. The spirit of the ROADMAP requirement ("readable and unobtrusive") is achieved with 12px uppercase tracking-wider semibold — visually prominent enough to read while remaining secondary to content. All other text uses existing sizes.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `--background` oklch(1 0 0) / dark: oklch(0.145 0 0) | Page background, surfaces |
| Secondary (30%) | `--card` oklch(1 0 0) / dark: oklch(0.205 0 0) | Cards, sidebar, popovers |
| Accent (10%) | `--primary` oklch(0.205 0 0) / dark: oklch(0.922 0 0) | CTA buttons, primary links, focus rings |
| Destructive | `--destructive` oklch(0.577 0.245 27.325) / dark: oklch(0.704 0.191 22.216) | Destructive actions only |

Accent reserved for: primary CTA buttons, active navigation, focus rings, "Start first session" link text (POL-07).

### Phase-Specific Color Decisions

| Item | Element | Spec |
|------|---------|------|
| POL-06 | Team card border in dark mode | Add `dark:border-border` class. Uses `--border: oklch(1 0 0 / 10%)` in dark mode -- a subtle white 10% opacity line providing definition against the dark background. |
| POL-07 | "Start first session" link | `text-primary` for the link text, matching existing CTA patterns in series cards. |

---

## Copywriting Contract

| Element | Copy (EN) | Copy (RO) | i18n Key |
|---------|-----------|-----------|----------|
| POL-01: Registration company placeholder | "Acme Corp" | "Acme SRL" | `auth.register.companyPlaceholder` |
| POL-07: Empty series CTA | "Start first session" (followed by arrow character) | "Incepe prima sesiune" (followed by arrow character) | `series.startFirst` (new key) |
| POL-08: History search placeholder | "Search..." | "Cauta..." | `history.searchPlaceholder` |

### Acronym Casing (POL-02)

Not a copywriting element per se, but a display formatter fix. The `formatActionLabel()` fallback must recognize these acronyms and render them in all-caps:

| Acronym | Correct Display |
|---------|-----------------|
| ai | AI |
| rls | RLS |
| api | API |

### States

| State | Element | Behavior |
|-------|---------|----------|
| Empty state | Series card with no sessions (POL-07) | Show "Start first session" link below AI summary placeholder. Visible to manager role only. Hidden for reports. |
| All-active state | People list status column (POL-04) | Return null (empty cell) when user status is "active". Badge renders only for "pending" or "deactivated" statuses. |
| Error state | N/A | No error states introduced in this phase. |
| Destructive action | N/A | No destructive actions introduced in this phase. |

---

## Component Inventory

No new components. All changes are modifications to existing components:

| Component | File | Changes |
|-----------|------|---------|
| Register page | `src/app/(auth)/register/page.tsx` | Placeholder text via i18n (POL-01) |
| Audit log client | `src/app/(dashboard)/settings/audit-log/audit-log-client.tsx` | Acronym map in `formatActionLabel()` (POL-02) |
| Forgot password page | `src/app/(auth)/forgot-password/page.tsx` | Verify vertical centering (POL-03, likely no-op) |
| People table columns | `src/components/people/people-table-columns.tsx` | Conditional badge render (POL-04) |
| Action items page | `src/components/action-items/action-items-page.tsx` | Font size change at 2 locations (POL-05) |
| Team card | `src/components/people/team-card.tsx` | Dark mode border class (POL-06) |
| Series card | `src/components/series/series-card.tsx` | Empty state link (POL-07) |
| History page | `src/components/history/history-page.tsx` | Placeholder text via i18n (POL-08) |
| EN auth translations | `messages/en/auth.json` | POL-01 |
| RO auth translations | `messages/ro/auth.json` | POL-01 |
| EN history translations | `messages/en/history.json` | POL-08 |
| RO history translations | `messages/ro/history.json` | POL-08 |
| EN series translations | `messages/en/series.json` or equivalent | POL-07 (new key) |
| RO series translations | `messages/ro/series.json` or equivalent | POL-07 (new key) |

---

## Interaction Contracts

### POL-07: "Start first session" link

| Property | Spec |
|----------|------|
| Element | Button styled as text link (`text-xs text-primary`) |
| Click behavior | Calls `startSession.mutate()` (existing mutation in series-card.tsx) |
| Click propagation | Must include `relative z-10 pointer-events-auto` and call `e.preventDefault(); e.stopPropagation()` to avoid triggering the card's full-area Link overlay |
| Visibility | Manager only (`isManager` flag, already computed in component) AND no `latestSession` |
| Position | Below the AI summary placeholder text |
| Hover | Underline via `hover:underline` |

### POL-03: Forgot-password centering

| Property | Spec |
|----------|------|
| Expected state | Already centered via shared `(auth)/layout.tsx` which applies `flex min-h-screen items-center justify-center` |
| Action | Verify visually. If centered, document as verified no-op. If not, replicate the container classes from login page. |

### POL-04: Active badge conditional

| Property | Spec |
|----------|------|
| Logic | Per-row check: `if (status === "active") return null` |
| Rationale | "Badge only appears for non-default states" means active users never show a badge, regardless of whether other users are active or not |
| Column header | Status column header remains visible; only cell content is conditional |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Card, Badge, Separator (already installed) | not required |

No new shadcn components to install. No third-party registries.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
