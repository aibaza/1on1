---
phase: 25-core-api-business-logic
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/validations/__tests__/correction.test.ts
  - src/lib/auth/__tests__/rbac.test.ts
  - src/lib/ai/schemas/__tests__/correction.test.ts
  - src/lib/utils/__tests__/scoring.test.ts
autonomous: true
requirements: [WFLOW-01, WFLOW-02, WFLOW-03, ANLT-01]

must_haves:
  truths:
    - "correctionInputSchema rejects reasons shorter than 20 chars, longer than 500 chars, and missing answerId — verified by failing tests"
    - "canCorrectSession logic is tested for admin, own-series manager, other-series manager, and member — all failing before implementation"
    - "reasonValidationResultSchema structure is tested for required pass/feedback fields — failing before AI schema exists"
    - "computeSessionScore produces correct weighted average when a numeric answer changes — failing before scoring test file exists"
  artifacts:
    - path: "src/lib/validations/__tests__/correction.test.ts"
      provides: "RED tests for correctionInputSchema and validateReasonSchema Zod validation"
      min_lines: 40
    - path: "src/lib/auth/__tests__/rbac.test.ts"
      provides: "RED tests for canCorrectSession (extended from existing rbac.test.ts)"
      min_lines: 30
    - path: "src/lib/ai/schemas/__tests__/correction.test.ts"
      provides: "RED tests for reasonValidationResultSchema shape"
      min_lines: 25
    - path: "src/lib/utils/__tests__/scoring.test.ts"
      provides: "RED tests for computeSessionScore with multiple answer types and weights"
      min_lines: 30
  key_links:
    - from: "src/lib/validations/__tests__/correction.test.ts"
      to: "src/lib/validations/correction.ts"
      via: "import (file does not exist yet — tests must fail)"
      pattern: "from.*validations/correction"
    - from: "src/lib/auth/__tests__/rbac.test.ts"
      to: "src/lib/auth/rbac.ts"
      via: "import canCorrectSession (function does not exist yet)"
      pattern: "canCorrectSession"
    - from: "src/lib/ai/schemas/__tests__/correction.test.ts"
      to: "src/lib/ai/schemas/correction.ts"
      via: "import (file does not exist yet — tests must fail)"
      pattern: "from.*ai/schemas/correction"
---

<objective>
Write failing (RED) unit tests for all four behavioral contracts that Phase 25 must satisfy: Zod input validation, RBAC authorization, AI result schema shape, and session score computation.

Purpose: Establish the test scaffold before implementation so each implementation task in Wave 1 has a clear pass/fail signal. Tests must fail at the start of this plan and pass after Wave 1 implementation.
Output: Four test files — all tests RED, suite reports failures, no implementation code written.
</objective>

<execution_context>
@/home/dc/.claude/get-shit-done/workflows/execute-plan.md
@/home/dc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-core-api-business-logic/25-RESEARCH.md
@.planning/phases/25-core-api-business-logic/25-VALIDATION.md
@.planning/phases/24-schema-foundation/24-01-SUMMARY.md

<interfaces>
<!-- Key contracts the tests must import from (files do not exist yet — tests must fail on import). -->

From src/lib/auth/rbac.ts (existing — canCorrectSession does NOT exist yet):
```typescript
export function isAdmin(role: string): boolean
export function isSeriesParticipant(userId: string, series: { managerId: string; reportId: string }): boolean
// NOT YET PRESENT — canCorrectSession must be added in Plan 02:
// export function canCorrectSession(userId: string, userRole: string, series: { managerId: string }): boolean
```

From src/lib/utils/scoring.ts (existing):
```typescript
export function computeSessionScore(
  answers: Array<{ answerType: string; answerNumeric: number | null; skipped: boolean; scoreWeight: number }>
): number | null
```

Target files that do NOT exist yet (tests will import from these and fail):
- src/lib/validations/correction.ts  → correctionInputSchema, validateReasonSchema
- src/lib/ai/schemas/correction.ts   → reasonValidationResultSchema
</interfaces>
</context>

<feature>
  <name>Phase 25 RED test scaffold — 4 test files, all failing</name>
  <files>
    src/lib/validations/__tests__/correction.test.ts,
    src/lib/auth/__tests__/rbac.test.ts,
    src/lib/ai/schemas/__tests__/correction.test.ts,
    src/lib/utils/__tests__/scoring.test.ts
  </files>
  <behavior>
    Correction input validation (correctionInputSchema):
    - rejects reason with 19 chars (below minimum 20) → ZodError
    - rejects reason with 501 chars (above maximum 500) → ZodError
    - accepts reason with exactly 20 chars → passes
    - accepts reason with exactly 500 chars → passes
    - rejects missing answerId → ZodError
    - rejects non-UUID answerId → ZodError
    - accepts valid UUID answerId → passes
    - accepts optional newAnswerText, newAnswerNumeric, newAnswerJson, skipped

    Validate-reason schema (validateReasonSchema):
    - rejects reason with 19 chars → ZodError
    - accepts reason with 20 chars → passes

    RBAC — canCorrectSession:
    - returns true for admin role regardless of series.managerId
    - returns true for manager role when userId === series.managerId
    - returns false for manager role when userId !== series.managerId
    - returns false for member role even when userId === series.managerId

    AI result schema (reasonValidationResultSchema):
    - accepts { pass: true, feedback: "Good reason." } → passes
    - accepts { pass: false, feedback: "Missing specifics." } → passes
    - rejects missing pass field → ZodError
    - rejects missing feedback field → ZodError
    - rejects feedback longer than 200 chars → ZodError

    Scoring (computeSessionScore):
    - returns null when no scorable answers exist
    - returns correct weighted average for mixed rating_1_5 answers
    - handles rating_1_10 normalization to 1-5 scale
    - handles yes_no (1 → 5, 0 → 1)
    - returns null when all answers are skipped
  </behavior>
  <implementation>
    Write tests that import from the target modules. For modules that do not exist yet
    (correction.ts validations, AI schemas/correction.ts), the test files will fail at
    the import stage — this is the expected RED state. For existing modules (rbac.ts,
    scoring.ts), import the existing functions and add the new test cases — these will
    fail because canCorrectSession does not exist in rbac.ts yet.

    RED commit: test(25-01): add failing tests for correction validation, RBAC, AI schema, scoring
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests — Zod schemas and AI result schema</name>
  <files>
    src/lib/validations/__tests__/correction.test.ts,
    src/lib/ai/schemas/__tests__/correction.test.ts
  </files>
  <behavior>
    - correctionInputSchema rejects reason 19 chars → ZodError
    - correctionInputSchema accepts reason 20 chars → passes
    - correctionInputSchema rejects reason 501 chars → ZodError
    - correctionInputSchema accepts reason 500 chars → passes
    - correctionInputSchema rejects missing answerId → ZodError
    - correctionInputSchema rejects non-UUID answerId ("not-a-uuid") → ZodError
    - correctionInputSchema accepts full valid input → passes
    - validateReasonSchema rejects reason 19 chars → ZodError
    - validateReasonSchema accepts reason 20 chars → passes
    - reasonValidationResultSchema accepts { pass: true, feedback: "Good." } → passes
    - reasonValidationResultSchema rejects missing pass → ZodError
    - reasonValidationResultSchema rejects feedback > 200 chars → ZodError
  </behavior>
  <action>
    Create src/lib/validations/__tests__/correction.test.ts:
    ```
    import { describe, it, expect } from 'vitest';
    import { correctionInputSchema, validateReasonSchema } from '../correction';
    import { ZodError } from 'zod';
    // ... tests importing from the non-existent file
    ```
    Create src/lib/ai/schemas/__tests__/correction.test.ts:
    ```
    import { describe, it, expect } from 'vitest';
    import { reasonValidationResultSchema } from '../correction';
    import { ZodError } from 'zod';
    // ... tests importing from the non-existent file
    ```
    Both files import from modules that do not exist — tests will fail with import errors.
    This is the expected RED state.

    Use `import { ZodError } from 'zod'` and `.safeParse()` to test Zod schemas.
    Check failures with `result.success === false` and `result.error instanceof ZodError`.

    RED commit: test(25-01): add failing correction Zod schema tests
  </action>
  <verify>
    <automated>cd /home/dc/work/1on1 && bun run test src/lib/validations/__tests__/correction.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>Both test files exist on disk. Running `bun run test` on these files shows failures (import errors or test failures) — NOT zero tests collected. Tests must be collected and fail, not be skipped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing tests — RBAC canCorrectSession and scoring</name>
  <files>
    src/lib/auth/__tests__/rbac.test.ts,
    src/lib/utils/__tests__/scoring.test.ts
  </files>
  <behavior>
    canCorrectSession:
    - returns true for admin with any managerId mismatch
    - returns true for manager when userId === series.managerId
    - returns false for manager when userId !== series.managerId
    - returns false for member role

    computeSessionScore:
    - returns null for empty answers array
    - returns null when all answers are skipped
    - returns 3 for a single rating_1_5 answer of value 3, weight 1
    - returns weighted average for two rating_1_5 answers with different weights
    - rating_1_10 answer of value 10 normalizes to 5 on the 1-5 scale
    - yes_no answer of value 1 normalizes to 5 on the 1-5 scale
    - yes_no answer of value 0 normalizes to 1 on the 1-5 scale
  </behavior>
  <action>
    Replace src/lib/auth/__tests__/rbac.test.ts with an extended version that keeps
    all existing `canManageTemplates` tests AND adds a new `describe('canCorrectSession', ...)` block.
    Import `canCorrectSession` from `'../rbac'` — this import will fail (function doesn't exist yet).

    IMPORTANT: Read the existing file first to preserve existing tests:
    Current content of src/lib/auth/__tests__/rbac.test.ts tests `canManageTemplates` with 5 cases.
    Add `canCorrectSession` import to the import line and a new describe block after the existing one.

    Create src/lib/utils/__tests__/scoring.test.ts — NEW file (directory does not exist):
    ```
    import { describe, it, expect } from 'vitest';
    import { computeSessionScore } from '../scoring';
    // ... tests for scoring behavior
    ```
    The scoring function EXISTS in scoring.ts — these tests may or may not pass.
    Run them to discover current state. Document RED/GREEN in the commit message.

    RED commit: test(25-01): add canCorrectSession and scoring unit tests
  </action>
  <verify>
    <automated>cd /home/dc/work/1on1 && bun run test src/lib/auth/__tests__/rbac.test.ts src/lib/utils/__tests__/scoring.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>Both test files exist. rbac.test.ts fails due to missing canCorrectSession. scoring.test.ts either passes (existing implementation is complete) or fails with specific assertion failures. Either outcome is valid RED — no implementation written in this plan.</done>
</task>

</tasks>

<verification>
After both tasks:
1. Run `bun run test src/lib/validations/__tests__/correction.test.ts src/lib/ai/schemas/__tests__/correction.test.ts` — must report failures (import errors from missing modules)
2. Run `bun run test src/lib/auth/__tests__/rbac.test.ts` — must report failure for canCorrectSession tests (canManageTemplates tests should still pass)
3. All test files exist on disk with meaningful test cases
4. No implementation files created in this plan
</verification>

<success_criteria>
- Four test files committed and on disk
- Running the test suite reports failures (RED) — not "no tests found"
- Existing canManageTemplates tests in rbac.test.ts remain passing
- No source implementation files touched
- Commit exists: test(25-01): add failing tests for correction validation, RBAC, AI schema, scoring
</success_criteria>

<output>
After completion, create `.planning/phases/25-core-api-business-logic/25-01-SUMMARY.md`
</output>
