---
phase: 22-safety-errors-inputs
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/components/ui/__tests__/date-picker.test.tsx
  - src/components/teams/__tests__/team-detail-danger-zone.test.tsx
  - src/components/ui/calendar.tsx
  - package.json
autonomous: true
requirements:
  - SAFE-01
  - INP-01
must_haves:
  truths:
    - "SAFE-01 unit test file exists and runs RED (import-only, component file not yet created)"
    - "INP-01 unit test file exists and runs RED (import-only, DatePicker not yet created)"
    - "react-day-picker and date-fns packages are installed in package.json"
    - "calendar.tsx is present in src/components/ui/ (installed via shadcn CLI)"
  artifacts:
    - path: "src/components/ui/__tests__/date-picker.test.tsx"
      provides: "Failing tests for DatePicker string<>Date conversion contract"
      min_lines: 30
    - path: "src/components/teams/__tests__/team-detail-danger-zone.test.tsx"
      provides: "Failing tests for Danger Zone AlertDialog presence"
      min_lines: 25
    - path: "src/components/ui/calendar.tsx"
      provides: "shadcn Calendar component (react-day-picker v9)"
  key_links:
    - from: "src/components/ui/__tests__/date-picker.test.tsx"
      to: "src/components/ui/date-picker.tsx"
      via: "import (does not exist yet — test must fail)"
      pattern: "from.*date-picker"
    - from: "src/components/teams/__tests__/team-detail-danger-zone.test.tsx"
      to: "src/app/(dashboard)/teams/[id]/team-detail-client.tsx"
      via: "import (file exists, component refactor not done — assertions must fail)"
      pattern: "from.*team-detail-client"
---

<objective>
Wave 0 TDD setup for Phase 22: install dependencies, add shadcn calendar.tsx, and write failing tests that define the behavioral contracts for SAFE-01 (Danger Zone AlertDialog) and INP-01 (DatePicker string conversion). No implementation yet — RED is the target state.

Purpose: Establish test scaffolds before implementation so Wave 1 plans have clear pass/fail targets and cannot ship incomplete work.
Output: Two failing test files, calendar.tsx installed, react-day-picker + date-fns in package.json.
</objective>

<execution_context>
@/home/dc/.claude/get-shit-done/workflows/execute-plan.md
@/home/dc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-safety-errors-inputs/22-RESEARCH.md
@.planning/phases/22-safety-errors-inputs/22-VALIDATION.md

<interfaces>
<!-- Key contracts extracted from codebase for executor context -->

From src/app/(dashboard)/teams/[id]/team-detail-client.tsx (lines 1-50, 332-358):
```tsx
// Existing delete button — uses variant="destructive" inline with "Add Members"
// Currently uses confirm() — to be replaced with AlertDialog in plan 22-02
{isAdmin && (
  <Button
    variant="destructive"
    size="sm"
    onClick={() => {
      if (confirm(t("deleteConfirm"))) {
        deleteTeamMutation.mutate();
      }
    }}
    disabled={deleteTeamMutation.isPending}
  >
    <Trash2 className="mr-2 h-4 w-4" />
    {deleteTeamMutation.isPending ? t("deleting") : t("deleteTeam")}
  </Button>
)}
```

DatePicker behavioral contract (defined by INP-01 success criteria):
- Accepts `value: string` (YYYY-MM-DD or "")
- Calls `onChange(value: string)` with YYYY-MM-DD when date selected
- Calls `onChange("")` when date cleared
- Renders a trigger button (not a native input)
- Does NOT render `<input type="date">`

AlertDialog behavioral contract (SAFE-01):
- `AlertDialog` component wraps the delete trigger
- Trigger button uses `variant="outline"` with destructive classes (NOT `variant="destructive"`)
- Delete button is inside a section with "Danger Zone" heading text
- Section appears AFTER the non-destructive actions row
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dependencies and add calendar.tsx</name>
  <files>package.json, src/components/ui/calendar.tsx</files>
  <action>
Run in this exact order:
1. `bun add react-day-picker date-fns` — installs react-day-picker v9 (latest) and date-fns v4
2. `bunx shadcn@latest add calendar --yes` — writes src/components/ui/calendar.tsx using the new-york style matching the project's components.json

If the shadcn CLI fails (network or version issue), write calendar.tsx manually using the react-day-picker v9 DayPicker API as shown in the RESEARCH.md code examples section. The manual version must export `Calendar` and `CalendarProps`.

After installation, confirm: `grep "react-day-picker" package.json` shows v9.x.
  </action>
  <verify>
    <automated>grep -r "react-day-picker" /home/dc/work/1on1/package.json && test -f /home/dc/work/1on1/src/components/ui/calendar.tsx</automated>
  </verify>
  <done>package.json contains react-day-picker (^9.x) and date-fns; src/components/ui/calendar.tsx exists and exports Calendar</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing test — INP-01 DatePicker contract</name>
  <files>src/components/ui/__tests__/date-picker.test.tsx</files>
  <behavior>
    - Test 1: DatePicker renders a button trigger element (role="button"), not a native date input
    - Test 2: When value="" is passed, the trigger shows the placeholder text
    - Test 3: When value="2026-03-15" is passed, the trigger shows a formatted date string (not "2026-03-15" verbatim — date-fns formats it)
    - Test 4: Selecting a date on the Calendar calls onChange with a YYYY-MM-DD string
  </behavior>
  <action>
Create `src/components/ui/__tests__/date-picker.test.tsx`. Add `// @vitest-environment happy-dom` at the top (required for React component rendering — project global env is node).

Import `DatePicker` from `@/components/ui/date-picker` (file does not exist yet — this import MUST cause RED).

Write tests using vitest + @testing-library/react. Test the behavioral contract: trigger is a button, value="" shows placeholder, value="2026-03-15" formats the date for display, onChange emits YYYY-MM-DD strings.

Do NOT create src/components/ui/date-picker.tsx in this task — the goal is RED tests only.

After creating the file, run `bun run test --run src/components/ui/__tests__/date-picker.test.tsx` and confirm the output shows failing tests (import error is acceptable as RED). Do NOT proceed until tests fail.
  </action>
  <verify>
    <automated>cd /home/dc/work/1on1 && bun run test --run src/components/ui/__tests__/date-picker.test.tsx 2>&1 | grep -E "FAIL|Cannot find|failed|Error"</automated>
  </verify>
  <done>Test file exists; running it produces test failures (import error or assertion failures) — confirming RED state</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write failing test — SAFE-01 Danger Zone contract</name>
  <files>src/components/teams/__tests__/team-detail-danger-zone.test.tsx</files>
  <behavior>
    - Test 1: The rendered component contains text "Danger Zone" (or the i18n key equivalent)
    - Test 2: The delete trigger button uses variant="outline" with destructive color classes, NOT variant="destructive" (filled red)
    - Test 3: An AlertDialog component wraps the delete action (role="alertdialog" appears when triggered)
    - Test 4: The Danger Zone section appears after the "Add Members" button in DOM order
  </behavior>
  <action>
Create `src/components/teams/__tests__/team-detail-danger-zone.test.tsx`. Add `// @vitest-environment happy-dom` at the top.

Import `TeamDetailClient` from `src/app/(dashboard)/teams/[id]/team-detail-client` (file EXISTS — tests run but assertions fail because the component is not yet refactored).

Mock next-intl useTranslations to return key-as-string (t(key) => key). Mock @tanstack/react-query useMutation and useQuery to return stub data. Mock next/navigation useRouter.

Write tests that assert:
1. A heading with text "dangerZone" (the i18n key) exists in the DOM
2. The delete button renders with classes containing "border-destructive" and "text-destructive" (not a filled destructive variant)
3. The delete button is inside an AlertDialogTrigger (assert AlertDialog is present via dialog role)
4. The delete section appears below the "addMembers" button in DOM order

Run `bun run test --run src/components/teams/__tests__/team-detail-danger-zone.test.tsx` and confirm RED. The current component has `variant="destructive"` and `confirm()`, so assertions about `border-destructive` and AlertDialog will fail.
  </action>
  <verify>
    <automated>cd /home/dc/work/1on1 && bun run test --run src/components/teams/__tests__/team-detail-danger-zone.test.tsx 2>&1 | grep -E "FAIL|failed|Error|✗"</automated>
  </verify>
  <done>Test file exists; running it produces assertion failures — confirming RED state before plan 22-02 implementation</done>
</task>

</tasks>

<verification>
All three tasks complete when:
1. `grep "react-day-picker" package.json` shows v9.x entry
2. `test -f src/components/ui/calendar.tsx` passes
3. `bun run test --run src/components/ui/__tests__/date-picker.test.tsx` — FAILS (RED)
4. `bun run test --run src/components/teams/__tests__/team-detail-danger-zone.test.tsx` — FAILS (RED)
5. `bun run typecheck` passes (no TypeScript errors in the test files themselves)
</verification>

<success_criteria>
- react-day-picker v9 and date-fns v4 are in package.json
- src/components/ui/calendar.tsx exists and exports Calendar component
- Two failing test files exist defining the behavioral contracts for SAFE-01 and INP-01
- No passing tests are broken — only the two new test files fail
- bun run typecheck passes
</success_criteria>

<output>
After completion, create `.planning/phases/22-safety-errors-inputs/22-01-SUMMARY.md`
</output>
