---
phase: 14-romanian-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/ro/templates.json
  - messages/ro/teams.json
  - messages/ro/people.json
  - messages/ro/analytics.json
  - messages/ro/auth.json
  - messages/ro/settings.json
  - messages/ro/sessions.json
autonomous: true
requirements:
  - ROLN-01
  - ROLN-02
  - ROLN-03

must_haves:
  truths:
    - "All Romanian translation values use correct comma-below diacritics (no unaccented forms like Creeaza, adauga, sterge, actiune, intalniri)"
    - "All ICU plural keys in ro.json have explicit one/few/other forms — not just one/other"
    - "Count values 0, 2, 5, 19 render using the plural surface form (few category), while 1 renders singular and 20+ renders other category"
  artifacts:
    - path: "messages/ro/analytics.json"
      provides: "Analytics namespace with diacritic fixes and three-form ICU plurals"
      contains: "few {# membri}"
    - path: "messages/ro/templates.json"
      provides: "Templates namespace with all 39 diacritic fixes"
      contains: "Creează"
    - path: "messages/ro/sessions.json"
      provides: "Sessions namespace with ICU plural three-form fixes"
      contains: "few {# întrebări}"
  key_links:
    - from: "messages/ro/analytics.json"
      to: "Intl.PluralRules('ro').select(n)"
      via: "ICU MessageFormat few form"
      pattern: "few \\{# (membri|sesiuni|acțiuni|eșantioane)\\}"
    - from: "messages/ro/sessions.json"
      to: "Intl.PluralRules('ro').select(n)"
      via: "ICU MessageFormat few form"
      pattern: "few \\{# (întrebări|răspunsuri)"
---

<objective>
Fix all Romanian translation quality issues in ro.json files: apply correct comma-below diacritics across ~100 strings, and add the missing Romanian `few` plural form to all 6 ICU plural keys.

Purpose: Romanian users see correct, natural phrasing. Counts 0, 2, 5, 11, 19 render with the plural surface form ("sesiuni") as required by Romanian CLDR, not the singular form.
Output: 7 updated ro.json namespace files with correct diacritics and three-form ICU plurals.
</objective>

<execution_context>
@/home/dc/.claude/get-shit-done/workflows/execute-plan.md
@/home/dc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-romanian-quality/14-RESEARCH.md

<interfaces>
<!-- Romanian CLDR plural rules (authoritative via Node.js built-in) -->
<!-- Verify at any time: node -e "const r=new Intl.PluralRules('ro');[0,1,2,5,19,20,21,100].forEach(n=>console.log(n+': '+r.select(n)))" -->
<!-- Results: 0=few, 1=one, 2=few, 5=few, 19=few, 20=other, 21=other, 100=other -->

ICU three-form template for Romanian:
  "{count, plural, one {# SINGULAR} few {# PLURAL} other {# PLURAL}}"
  Note: few and other MAY have identical surface text — that is correct per CLDR spec.

Full diacritic replacement map (authoritative from RESEARCH.md):
  Creeaza → Creează    creeaza → creează
  Adauga → Adaugă      adauga → adaugă
  Anuleaza → Anulează  anuleaza → anulează
  Selecteaza → Selectează  selecteaza → selectează
  Cauta → Caută        cauta → caută
  Salveaza → Salvează  salveaza → salvează
  Elimina → Elimină    elimina → elimină
  Fara → Fără          fara → fără
  asigneaza → asignează
  noua → nouă          Noua → Nouă
  Inapoi → Înapoi      inapoi → înapoi
  incepe → începe      Incepe → Începe
  intalniri → întâlniri  intalnire → întâlnire
  inregistrat → înregistrat
  sterge → șterge      Sterge → Șterge
  stearsa → ștearsă    stergi → ștergi
  gasit → găsit        Gasit → Găsit
  actiune → acțiune    actiuni → acțiuni
  esantion → eșantion  esantioane → eșantioane
  aderenta → aderență  diferenta → diferență
  comparatie → comparație  Comparatie → Comparație
  tendinta → tendința  Tendinta → Tendința
  optiune → opțiune    optiuni → opțiuni
  informatii → informații  necesita → necesită

ICU plural keys that need three-form fix (currently only one/other):
  analytics.json:
    memberCount: currently "one {# membru} other {# membri}"
    sessionCount: currently "one {# sesiune} other {# sesiuni}"
    itemsCompleted: currently "one {# actiune} other {# actiuni}} finalizate" (also needs diacritic fix)
    sampleCount: currently "one {# esantion} other {# esantioane}" (also needs diacritic fix)
  sessions.json:
    questionCount: currently "one {1 întrebare} other {# întrebări}"
    answersRecorded: currently "one {1 răspuns înregistrat} other {# răspunsuri înregistrate}"

Files with highest diacritic issue counts:
  templates.json: 39 issues (Creeaza×6, Adauga×6, Elimina×7, Selecteaza×6, Anuleaza×5)
  teams.json: 24 issues (Creeaza×5, Adauga×3, sterge/Sterge/stearsa/stergi)
  people.json: 18 issues (Adauga×2+3, Anuleaza×2, Selecteaza×2, Cauta×3, gasit×2)
  analytics.json: 16 issues (actiune/actiuni×4, esantion/esantioane×3, intalniri×2, Tendinta)
  auth.json: 2 issues (noua×2)
  settings.json: 1 issue (elimina×1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix diacritics and ICU plurals in analytics.json and sessions.json</name>
  <files>messages/ro/analytics.json, messages/ro/sessions.json</files>
  <action>
Read both files fully before making any changes.

In messages/ro/analytics.json:
1. Apply ALL diacritic replacements from the replacement map to every string value in the file. Specifically confirm: actiune→acțiune, actiuni→acțiuni, esantion→eșantion, esantioane→eșantioane, intalniri→întâlniri, intalnire→întâlnire, Tendinta→Tendința, aderenta→aderență, diferenta→diferență, comparatie→comparație, Comparatie→Comparație, necesita→necesită, optiune→opțiune, optiuni→opțiuni.

2. Fix 4 ICU plural keys — add `few` form:
   - memberCount: "{count, plural, one {# membru} few {# membri} other {# membri}}"
   - sessionCount: "{count, plural, one {# sesiune} few {# sesiuni} other {# sesiuni}}"
   - itemsCompleted: "{count, plural, one {# acțiune} few {# acțiuni} other {# acțiuni}} finalizate"
   - sampleCount: "{count, plural, one {# eșantion} few {# eșantioane} other {# eșantioane}}"

In messages/ro/sessions.json:
1. Apply ALL diacritic replacements from the replacement map to every string value in the file.
2. Fix 2 ICU plural keys — add `few` form:
   - questionCount: "{count, plural, one {1 întrebare} few {# întrebări} other {# întrebări}}"
   - answersRecorded: "{count, plural, one {1 răspuns înregistrat} few {# răspunsuri înregistrate} other {# răspunsuri înregistrate}}"

CRITICAL: Do targeted key-level replacements, not global file-level search/replace. Read the current value of each key, apply the correct replacement, verify the result is valid JSON. Do NOT accidentally corrupt correct diacritics already present.
  </action>
  <verify>
    <automated>
node -e "
const fs = require('fs');
const bad = ['actiune','actiuni','esantion','esantioane','intalniri','Tendinta','aderenta','comparatie'];
['messages/ro/analytics.json','messages/ro/sessions.json'].forEach(f => {
  const c = fs.readFileSync('/home/dc/work/1on1/' + f, 'utf-8');
  JSON.parse(c); // validate JSON
  bad.forEach(b => { if(c.includes(b)) console.error('FAIL: ' + f + ' still contains: ' + b); });
  if(!c.includes('few')) console.error('FAIL: ' + f + ' missing few plural form');
  else console.log('OK: ' + f);
});
"
    </automated>
  </verify>
  <done>analytics.json and sessions.json are valid JSON; all 6 ICU plural keys have explicit one/few/other forms; known bad diacritic forms absent from both files</done>
</task>

<task type="auto">
  <name>Task 2: Fix diacritics in templates.json, teams.json, people.json, auth.json, and settings.json</name>
  <files>messages/ro/templates.json, messages/ro/teams.json, messages/ro/people.json, messages/ro/auth.json, messages/ro/settings.json</files>
  <action>
Read each file fully before making any changes. Apply the complete diacritic replacement map to every string value in each file. The files have NO ICU plural issues — this task is diacritic-only.

templates.json (39 issues): Fix Creeaza→Creează (×6), creeaza→creează, Adauga→Adaugă (×6), adauga→adaugă, Elimina→Elimină (×7), elimina→elimină, Selecteaza→Selectează (×6), selecteaza→selectează, Anuleaza→Anulează (×5), anuleaza→anulează. Also apply any other replacement map entries that appear (Salveaza, Fara, Inapoi, etc.).

teams.json (24 issues): Fix Creeaza→Creează (×5), creeaza→creează, Adauga→Adaugă (×3), adauga→adaugă, sterge→șterge, Sterge→Șterge, stearsa→ștearsă, stergi→ștergi. Also apply any other replacement map entries.

people.json (18 issues): Fix Adauga→Adaugă (×2+3), adauga→adaugă, Anuleaza→Anulează (×2), anuleaza→anulează, Selecteaza→Selectează (×2), selecteaza→selectează, Cauta→Caută (×3), cauta→caută, gasit→găsit (×2), Gasit→Găsit. Also apply any other replacement map entries.

auth.json (2 issues): Fix noua→nouă (×2). Also apply any other replacement map entries.

settings.json (1 issue): Fix elimina→elimină (×1). Also apply any other replacement map entries.

CRITICAL: Do targeted key-level replacements — read each key's value, apply replacement, verify the result. Do NOT corrupt correct diacritics already present in the strings (e.g., if "ă" already exists in a word, do not double-apply). After editing all 5 files, run the diacritic audit script from RESEARCH.md to confirm zero bad forms remain.

Post-edit verification script:
node -e "
const fs = require('fs'), path = require('path');
const bad = ['Creeaza','creeaza','Adauga','adauga','Anuleaza','Selecteaza',
             'sterge','Sterge','stearsa','stergi','gasit','Gasit','actiune','actiuni',
             'esantion','aderenta','comparatie','Tendinta','optiune','Inapoi','intalniri',
             'necesita','noua','elimina','Salveaza','salveaza','Cauta','cauta','Fara','fara'];
const dir = '/home/dc/work/1on1/messages/ro';
let total = 0;
fs.readdirSync(dir).filter(f => f.endsWith('.json')).forEach(fn => {
  const content = fs.readFileSync(path.join(dir, fn), 'utf-8');
  bad.forEach(b => { const c = (content.match(new RegExp('\\\\b' + b + '\\\\b', 'g')) || []).length; if(c) { console.log(fn + ': ' + c + 'x ' + b); total += c; }});
});
console.log('Total bad forms remaining: ' + total);
"
  </action>
  <verify>
    <automated>
node -e "
const fs = require('fs'), path = require('path');
const bad = ['Creeaza','creeaza','Adauga','adauga','Anuleaza','anuleaza','Selecteaza','selecteaza',
             'Sterge','stearsa','stergi','Gasit','gasit','noua ','elimina '];
const files = ['templates.json','teams.json','people.json','auth.json','settings.json'];
let ok = true;
files.forEach(fn => {
  const content = fs.readFileSync(path.join('/home/dc/work/1on1/messages/ro', fn), 'utf-8');
  try { JSON.parse(content); } catch(e) { console.error('INVALID JSON: ' + fn); ok = false; return; }
  bad.forEach(b => { if(content.includes(b)) { console.error('FAIL ' + fn + ': contains ' + b); ok = false; } });
});
if(ok) console.log('OK: all 5 files pass diacritic check and are valid JSON');
"
    </automated>
  </verify>
  <done>All 5 files are valid JSON with all identified diacritic errors corrected; diacritic audit script reports 0 bad forms in those files; correct diacritics already present (ă, î, â, ș, ț) are untouched</done>
</task>

</tasks>

<verification>
Run full diacritic audit across all ro.json files:
```bash
cd /home/dc/work/1on1 && node -e "
const fs = require('fs'), path = require('path');
const bad = ['Creeaza','creeaza','Adauga','adauga','Anuleaza','Selecteaza',
             'sterge','Sterge','stearsa','stergi','gasit','actiune','actiuni',
             'esantion','aderenta','comparatie','Tendinta','optiune','Inapoi','intalniri',
             'necesita','noua','elimina'];
const dir = 'messages/ro';
let total = 0;
fs.readdirSync(dir).filter(f => f.endsWith('.json')).forEach(fn => {
  const content = fs.readFileSync(path.join(dir, fn), 'utf-8');
  bad.forEach(b => { const c = (content.match(new RegExp(b, 'g')) || []).length; if(c) { console.log(fn + ': ' + c + 'x ' + b); total += c; }});
});
console.log('Total bad forms: ' + total);
"
```
Expected: 0 bad forms.

Verify all 6 ICU plural keys have `few` form:
```bash
grep -l "few" /home/dc/work/1on1/messages/ro/analytics.json /home/dc/work/1on1/messages/ro/sessions.json
```
Expected: both files listed.

Verify JSON validity of all ro.json files:
```bash
cd /home/dc/work/1on1 && for f in messages/ro/*.json; do node -e "JSON.parse(require('fs').readFileSync('$f','utf-8'))" && echo "OK: $f" || echo "INVALID: $f"; done
```
</verification>

<success_criteria>
- All 927 Romanian translation keys are valid JSON
- Diacritic audit script reports 0 bad forms across all 16 ro.json files
- analytics.json and sessions.json contain explicit `few` forms in all 6 ICU plural keys
- Romanian text for counts 0, 2, 5, 19 renders the correct plural surface form
</success_criteria>

<output>
After completion, create `.planning/phases/14-romanian-quality/14-01-SUMMARY.md` using the summary template.
</output>
