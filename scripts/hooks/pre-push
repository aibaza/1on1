#!/bin/bash
# Pre-push hook: typecheck only
# E2E tests run in CI: on PRs to main, pushes to develop, and manual dispatch
# Run locally on demand: npx playwright test --project=setup --project=chromium
# Skip with: git push --no-verify
set -e

echo "→ Typecheck..."
bun run typecheck
