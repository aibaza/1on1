#!/bin/bash
# Pre-push hook: typecheck only
# E2E tests run manually on demand: npx playwright test --project=setup --project=chromium
# Skip with: git push --no-verify
set -e

echo "→ Typecheck..."
bun run typecheck
